# ── Rocket Alert PWA Configuration ──

# Port (both internal and external)
PORT=3088
HOST_PORT=3088

# Optional: set a default home city (users can always change in the UI)
# DEFAULT_CITY=תל אביב - יפו

# Polling interval in milliseconds (2000 = 2 seconds)
POLL_INTERVAL=2000
